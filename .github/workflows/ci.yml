name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install AVR toolchain
        run: sudo apt-get update && sudo apt-get install -y gcc-avr avr-libc
      - name: Compile
        working-directory: PARKMicrochipstudio/park
        run: avr-gcc -mmcu=atmega328p -Os -Wall -c *.c
